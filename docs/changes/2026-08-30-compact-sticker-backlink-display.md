# 0.3.21 - Compact sticker backlink display

- Render managed sticker backlinks as one compact `DSH 贴纸` chip in Live Preview and Reading View.
- Keep the existing `obsidian://deepharness` target on the chip so navigation behavior is unchanged.
- Leave Source Mode, managed backlink metadata, deletion synchronization, and lifecycle behavior unchanged.
