# Obsidian 接收页面隔离与 RC2 配套发布

仅允许当前 Vault 配置的 Obsidian Web Viewer 领取新引用。队列读取按持久化页面标识和实例过滤，领取接口对重试及已完成操作再次验证页面身份；未配置接收页面时保留待处理引用。独立 DSH 窗口继续支持原有导航、回链与删除。使用已有 webViewerSurfaceId，不迁移或复制会话与引用数据。

验证：65 项 Bridge HTTP、接收限制和插件生命周期测试通过；类型检查与构建通过。组合测试验证独立窗口与 Obsidian 同实例并存时只向内嵌页会话写入引用。
